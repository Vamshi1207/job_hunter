version: "3.8"
services:
  pipeline:
    build: .
    volumes:
      - .:/app
      - ~/.gemini:/root/.gemini
    environment:
      - PYTHONUNBUFFERED=1
      - JOB_SEARCH_ROOT=/app
      - GEMINI_API_KEY=${GEMINI_API_KEY}
