# Application tracker

The pipeline appends a row after each tailored package. Copy this file to `applications/_tracker.md` if you want a header in place before the first run (optional — the pipeline creates the file as needed).

| Date | Company | Role | Channel | Status | Folder | Notes |
|---|---|---|---|---|---|---|
